name: Sync

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Push to public repo
      uses: ad-m/github-push-action@master
      with:
        github_token: '6bb18db7b1dd941e9ea21053735fd86e2a49304f'
        repository: 'BugSwarm/test-mirror-public'
